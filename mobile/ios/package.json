{
  "name": "be-mastery-ios",
  "private": true,
  "version": "1.1.0",
  "description": "BE Mastery — iOS app shell (Capacitor) around the web app in the repository root. See README.md.",
  "scripts": {
    "sync-web": "node scripts/sync-web.mjs",
    "sync": "node scripts/sync-web.mjs && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  }
}
