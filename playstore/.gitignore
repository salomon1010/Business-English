# NEVER commit signing secrets or build artifacts to the public repo
android.keystore
*.apk
*.aab
*.idsig
build*.log
build.expect
app/
build/
.gradle/
*.jks

# Bubblewrap-generated Gradle project files (regenerated by `bubblewrap init`
# from twa-manifest.json — no need to version them)
build.gradle
settings.gradle
gradle.properties
gradle/
gradlew
gradlew.bat
manifest-checksum.txt
